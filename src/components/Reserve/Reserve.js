import React from 'react';

const Reserve = () => (
  <span style={{
    backgroundColor: '#18A2B8', color: 'white', padding: '1px 3px', borderRadius: '4px',
  }}
  >
    Reserve
  </span>
);

export default Reserve;
